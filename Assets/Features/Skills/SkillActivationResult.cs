namespace Features.Skills
{
    public class SkillActivationResult
    {
        public readonly bool IsSuccess;

        public SkillActivationResult(bool isSuccess)
        {
            IsSuccess = isSuccess;
        }
    }
}